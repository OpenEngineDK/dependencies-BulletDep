IF(EXISTS ${BULLET_DEPS_SRC_DIR})

SET(CMAKE_INSTALL_PREFIX ${BULLET_DEPS_BUILD_DIR} CACHE INTERNAL "" FORCE)

#SUBDIRS(${BULLET_DEPS_SRC_DIR})
INCLUDE(oe-bullet.cmake)
ENDIF(EXISTS ${BULLET_DEPS_SRC_DIR})
